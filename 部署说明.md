# 林业野外调查即时录入系统 — 部署说明

## 一、系统架构

```
浏览器（手机/平板）
    ↓ HTTPS
nginx (docker: bibook-nginx-prod)
    ↓ proxy_pass http://172.18.0.1:8090
gunicorn -w 2 -b 0.0.0.0:8090 wsgi:application
    ↓
gateway (DispatcherMiddleware)
    ├── mount "/"      → gateway home（统一首页）
    ├── mount "/hqz"   → 伐区作业设计表生成
    ├── mount "/forest"→ forest-data
    └── mount "/survey"→ 本项目（林业野外调查录入）
```

本项目作为 gateway 第4个应用挂载，与 hqz-p2 共享 gunicorn 进程和端口，无需独立部署服务。

## 二、服务器环境要求

| 组件 | 要求 | 现有状态 |
|------|------|----------|
| Python | ≥ 3.10 | 已有 /home/www/shared_venv/ |
| Flask | ≥ 2.0 | 已在 shared_venv |
| openpyxl | ≥ 3.0 | 已在 shared_venv |
| gunicorn | ≥ 20 | 已有 gateway.service |
| nginx | 已配置 HTTPS | forest.bibook.top |
| systemd --user | www linger=yes | 已有 |

**无需安装任何新依赖**——Flask + openpyxl 已在共享 venv 中。

## 三、首次部署步骤

### 步骤1：本地准备

确认文件结构完整：
```
0810-调查/
├── survey/
│   ├── __init__.py
│   ├── schema.py          # 五张表字段定义
│   ├── storage.py         # SQLite 数据层
│   ├── exporter.py        # xlsx 导出
│   ├── survey.db          # 运行后自动生成
│   ├── web/
│   │   ├── __init__.py
│   │   ├── app.py         # Flask 入口
│   │   ├── templates/{base,index}.html
│   │   └── static/{style.css, app.js}
│   └── gateway/
│       └── survey.appspec  # gateway 挂载配置
├── tests/                  # 自动化测试
├── deploy.sh               # 部署脚本
└── 部署说明.md
```

### 步骤2：本地测试

```bash
cd /Users/sux/Desktop/0810-调查

# 运行自动化测试
python3 -m pytest tests/ -v

# 本地启动验证
python3 -m survey.web.app 5060
# 浏览器打开 http://127.0.0.1:5060 确认页面正常
```

### 步骤3：部署到服务器

```bash
cd /Users/sux/Desktop/0810-调查

# 执行部署脚本
./deploy.sh
```

deploy.sh 执行内容：
1. `rsync` 同步 survey/ → 服务器 /home/www/bibook_deploy/apps/survey/survey/
2. `scp` 上传 survey.appspec → 服务器 gateway/apps_root/
3. `ssh` 重启 gateway.service

### 步骤4：验证部署

```bash
# 健康检查
curl https://forest.bibook.top/survey/healthz
# 预期: {"ok": true, "tables": 5, "db_exists": true}

# Schema 接口
curl https://forest.bibook.top/survey/api/schema | python3 -m json.tool | head -20

# 浏览器访问
# https://forest.bibook.top/survey
```

## 四、部署脚本详解

### deploy.sh

```bash
#!/usr/bin/env bash
set -euo pipefail
REMOTE=${REMOTE:-www@forest.bibook.top}
DEST=${DEST:-/home/www/bibook_deploy/apps/survey}
EXCLUDE="--exclude=__pycache__/ --exclude=*.pyc --exclude=*.db --exclude=.DS_Store"

# 1. 同步 survey/ 到服务器（排除缓存和数据库）
rsync -avz $EXCLUDE ./survey/ "$REMOTE:$DEST/survey/"

# 2. 同步 appspec 到 gateway apps_root（gateway 自动发现）
scp ./survey/gateway/survey.appspec \
    "$REMOTE:/home/www/bibook_deploy/apps/hqz-p2/gateway/apps_root/survey.appspec"

# 3. 重启 gateway（gunicorn 重载，自动扫描新 appspec）
ssh "$REMOTE" "systemctl --user restart gateway.service"
```

### survey.appspec

```yaml
code: survey              # 唯一标识
name: 林业野外调查即时录入系统
prefix: /survey            # URL 前缀 → https://forest.bibook.top/survey
root: /home/www/bibook_deploy/apps/survey   # 服务器代码路径
factory: survey.web.app:create_app           # Flask 应用工厂
static_url_path: /survey/static              # 静态文件路径
auth: none                                   # 暂无认证
description: 林业野外调查五表即时录入
enabled: true                                # gateway 启用此应用
```

### 服务器目录结构

```
/home/www/bibook_deploy/apps/survey/
└── survey/
    ├── __init__.py
    ├── schema.py
    ├── storage.py
    ├── exporter.py
    ├── survey.db          # 运行时自动创建
    └── web/
        ├── __init__.py
        ├── app.py
        ├── templates/
        └── static/

/home/www/bibook_deploy/apps/hqz-p2/gateway/
├── apps_root/
│   ├── forest-data.appspec
│   ├── hqz.appspec
│   ├── x2sum.appspec
│   └── survey.appspec     ← 新增
├── wsgi.py                # 自动扫描 apps_root/*.appspec
└── config.yaml
```

## 五、日常更新部署

代码修改后只需一条命令：

```bash
cd /Users/sux/Desktop/0810-调查
./deploy.sh
```

输出示例：
```
sending incremental file list
survey/web/static/app.js
survey/web/app.py

sent 12,345 bytes  received 56 bytes  ...
survey.appspec     100%  156     1.2KB/s   00:00
[deploy] survey synced to /home/www/bibook_deploy/apps/survey, gateway restarted
```

## 六、数据库说明

- **存储方式**：SQLite，文件位于 `survey/survey.db`
- **自动创建**：首次启动时 `storage.init_db()` 自动建表，无需手动操作
- **备份**：定期 `scp www@forest.bibook.top:/home/www/bibook_deploy/apps/survey/survey/db ./`
- **排除**：deploy.sh 排除 .db 文件，不会覆盖服务器数据

### 表结构

| 表名 | 用途 |
|------|------|
| projects | 项目（名称/创建人/乡镇/时间） |
| prefilled | 预填数据（黄色列，按行存 JSON） |
| records | 录入记录（白色列，按行存 JSON） |

## 七、本地开发

```bash
# 启动开发服务器（debug 模式）
cd /Users/sux/Desktop/0810-调查
python3 -m survey.web.app 5060

# 访问
open http://127.0.0.1:5060

# 运行测试
python3 -m pytest tests/ -v
```

## 八、使用流程

### 内勤人员（如范应祥）
1. 在系统中创建项目（如"华宁县2025人工造林验收"）
2. 按乡镇从数据库提取基础数据，通过 API 预填黄色列
3. 通知外业人员项目名称

### 外业人员
1. 手机浏览器打开 `https://forest.bibook.top/survey`
2. 选择项目，选择对应表格
3. 野外现场在底部输入栏填写数据
4. 点击"添加此行"保存（断网时存本地，有网自动同步）
5. 当日工作结束后，有网时数据自动上传

### 内勤汇总
1. 在系统中点击"导出"
2. 下载 Excel 文件，包含所有外业录入数据
3. 按原模板格式整理汇总

## 九、常见部署问题

### Q: gateway 重启后 /survey 返回 404？
A: 检查 `apps_root/survey.appspec` 是否存在且 `enabled: true`，gateway 启动时自动扫描。

### Q: 页面打开但样式/JS 丢失？
A: 检查 appspec 中 `static_url_path: /survey/static` 是否正确，gateway 的 `mount.py` 负责前缀适配。

### Q: 导出 xlsx 报错？
A: 确认 shared_venv 中有 openpyxl：`/home/www/shared_venv/bin/python3 -c "import openpyxl"`。

### Q: 数据库锁错误？
A: storage.py 已用全局锁 + WAL 模式，正常不会锁。如出现，重启 gateway：`ssh www@forest.bibook.top "systemctl --user restart gateway.service"`。
